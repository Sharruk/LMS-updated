🔐 Where does the admin username & password come from?

After the change, login will work like this:

Username = value you set in Replit Secrets

Password = value you set in Replit Secrets

NOT from code.

🪜 Step-by-step: How to change admin password in Replit
Step 1 — Open Secrets

In Replit:

Left side → Click 🔒 Secrets (Environment Variables)

Step 2 — Add these 3 values

Add:

Name:
ADMIN_USERNAME
Value:
myadmin

Add:

Name:
ADMIN_PASSWORD
Value:
MyStrongPass@2026

Add:

Name:
SECRET_KEY
Value:
random-secret-123456789

Step 3 — Save

That’s it.

🧑‍💻 Now how to login?

Open:

http://10.0.2.15:5000/admin

Type:

Username:
myadmin

Password:
MyStrongPass@2026

🤔 Why earlier error came?

This error:

RuntimeError: The session is unavailable because no secret key was set


Means:

Your app did NOT have:

app.secret_key = "something"


So session cannot store login state.

🛠 Tell Replit AI to fix everything automatically

Copy paste this prompt to Replit AI:

Writing

Fix admin login system in my Flask app:

Add secret key:
app.secret_key = os.environ.get("SECRET_KEY", "dev-secret")

Replace hardcoded admin login:
Remove:
username = "admin"
password = "admin123"

Use environment variables instead:
ADMIN_USERNAME = os.environ.get("ADMIN_USERNAME")
ADMIN_PASSWORD = os.environ.get("ADMIN_PASSWORD")

Update login check:
if username == ADMIN_USERNAME and password == ADMIN_PASSWORD:
session['is_admin'] = True

Keep /admin route same.
If not logged in → show login page.
If logged in → show admin dashboard.

Add comments explaining:

How to set ADMIN_USERNAME

How to set ADMIN_PASSWORD

How to set SECRET_KEY
in Replit Secrets.

🔒 Important (VERY IMPORTANT)

Do NOT use:

admin

admin123

password

123456

Use something like:

Username:
tn_portal_admin

Password:
Tn@Portal#Secure2026

🚀 After deploying in Render

Students will open:

yourwebsite.onrender.com


They will NOT see admin page.

Admin page only opens if someone goes to:

yourwebsite.onrender.com/admin


Then it will ask:

Username

Password

Without correct login → cannot enter.