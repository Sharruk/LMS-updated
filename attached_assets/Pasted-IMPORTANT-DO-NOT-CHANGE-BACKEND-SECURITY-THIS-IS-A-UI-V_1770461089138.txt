IMPORTANT: DO NOT CHANGE BACKEND SECURITY.
THIS IS A UI VISIBILITY FIX ONLY.

--------------------------------------------------
CURRENT ISSUE
--------------------------------------------------

- The Upload link is visible in the public navbar
- Clicking Upload correctly shows "Access Denied"
- Backend protection is WORKING
- UI visibility is WRONG

--------------------------------------------------
GOAL
--------------------------------------------------

Hide ALL admin-only links from public users
and show them ONLY inside the admin area.

--------------------------------------------------
WHAT TO CHANGE (TARGETED FIX)
--------------------------------------------------

1) PUBLIC NAVBAR
- Remove / hide the following links from the public navbar:
  - Upload
  - Admin
  - Dashboard
  - Manage Files
- Public navbar should show ONLY:
  - Home
  - Classes
  - Search

2) ADMIN NAVBAR / ADMIN LAYOUT
- Create or use a separate admin layout/template
- Show admin-only links ONLY inside admin pages:
  - Upload
  - Manage Files
  - Logout

3) CONDITIONAL RENDERING
- Do NOT rely on backend errors for UI control
- Use template-level checks (is_admin flag or admin session)
- If user is NOT admin:
  - Do NOT render Upload link at all

4) ROUTING (DO NOT CHANGE)
- Keep /upload route protected
- Keep access denied behavior unchanged
- Do NOT weaken security

--------------------------------------------------
ADMIN ACCESS (CLARITY)
--------------------------------------------------

- Admin page must be accessible only via:
  /admin  OR  /_admin_secret
- Do NOT link admin page anywhere publicly
- Admin manually types the URL to access it

--------------------------------------------------
IMPORTANT RULES
--------------------------------------------------

- DO NOT delete routes
- DO NOT change authentication logic
- DO NOT break upload functionality
- ONLY adjust navbar visibility and admin layout

--------------------------------------------------
FINAL EXPECTATION
--------------------------------------------------

After this fix:
- Public users NEVER see Upload
- Public users never click admin links
- Admin sees Upload ONLY inside admin pages
- Access denied errors disappear for public users
